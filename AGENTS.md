# AGENTS.md

## 프로젝트 목표

이 별도 게임 저장소에서 js13kGames 2026 출품작을 만든다.

## 에이전트 키트

- 키트 출처: `/Users/harry/Documents/Github/game/js13k-agent-kit`
- 키트 버전: `fba1c6a35b548b243379285f59e5ef1b90c071fb`
- 작업 방식: 키트 저장소에서 이 외부 게임 저장소를 작업 대상으로 제어한다.

## 현재 테마

`Unicorns and Rainbows`

## 게임 콘셉트

유니콘을 사방으로 움직여 스테이지에 흩어진 프리즘 조각을 모두 모은다. 마지막 조각을 얻으면 무지개 포털이 열리고, 영구 능력 하나를 선택한 뒤 다음 스테이지로 이동한다. 적 처치는 목표가 아니며 모든 스테이지는 회피만으로도 완료 가능해야 한다.

## 공식 규칙

- 규칙 URL: https://js13kgames.com/2026/rules
- 규칙 확인 일시와 시간대: 2026-08-23 21:14 KST (+09:00)
- 대회 연도: 2026
- 자료 URL: https://js13kgames.com/resources

공식 규칙 페이지를 최종 기준으로 삼는다. 확인된 규칙과 가정을 구분한다.

## 확인된 제약

- 테마: `Unicorns and Rainbows`. 테마는 평가 기준이며 자유롭게 해석할 수 있다.
- 최종 ZIP 크기 제한: `.zip` 아카이브 `13,312`바이트 이하.
- 패키지 루트에 `index.html`이 있어야 하며 압축 해제 후 브라우저에서 바로 실행되어야 한다.
- 제출 기간: 2026-08-13 13:00 CEST부터 2026-09-13 13:00 CEST까지.
- 투표 기간: 2026-09-14부터 2026-10-04까지.
- 기본 부문: Desktop, Mobile. 추가 부문: Online, WebXR.
- 기본 부문은 코드, 데이터와 모든 자산을 ZIP 내부에 포함하며 외부 런타임 자원에 의존하지 않는다.
- TypeScript, WebAssembly, Rust, WebGL, WebGPU를 사용할 수 있으나 제출 패키지에는 브라우저 실행 결과물만 포함한다.
- 최신 Chrome과 Firefox에서 플레이 가능해야 하며 콘솔 오류가 없어야 한다.
- 외부 글꼴과 분석·추적 서비스는 금지한다. 포함하는 모든 자산의 사용 및 게시 권리를 보유해야 한다.
- `localStorage` 키에는 고유 namespace를 사용하고 `localStorage.clear()`를 호출하지 않는다.
- 제출물은 playable ZIP과 전체 빌드가 가능한 readable GitHub 저장소를 함께 제공한다.
- 제출에는 GitHub 계정이 필요하다.

## 미해결 질문과 가정

- Unfinished 제출과 Bugfix 단계는 2026-09-14만 명시되어 있고 정확한 마감 시각은 확인되지 않았다.
- 투표 시작과 종료 날짜의 정확한 시각 및 시간대는 확인되지 않았다.
- CEST에서 KST로 변환한 접수 기간은 2026-08-13 20:00 KST부터 2026-09-13 20:00 KST까지다.
- Online 부문 규칙은 공식적으로 변경 가능성이 있으므로 해당 부문을 선택한다면 구현 직전에 다시 확인한다.
- 현재 콘셉트는 외부 의존성이 없는 Desktop/Mobile 기본 부문을 목표로 한다.

## 필수 제약

- 최종 배포 ZIP을 13,312바이트 이하로 유지한다.
- 현재 규칙이 명시적으로 허용하지 않는 외부 런타임 의존성을 추가하지 않는다.
- 네트워크 요청, 원격 자산, CDN 스크립트, 원격 글꼴 또는 호스팅된 오디오에 의존하지 않는다.
- 디버그 도구, 편집기, 소스 맵, 테스트 도우미를 배포 결과에서 제외한다.
- 게임플레이, 콘텐츠, 시각 요소 또는 오디오를 의미 있게 바꿀 때마다 배포 ZIP 크기를 보고한다.
- 반복 개발 중에도 플레이 가능한 상태를 유지한다.

## TypeScript ECS 아키텍처

- 게임 소스는 strict TypeScript로 작성하고 `npm run check`를 통과한다.
- Entity는 브랜드 숫자 ID이며 데이터나 행동을 갖지 않는다.
- Component는 데이터 전용 인터페이스다.
- 데이터 Component는 `Map<Entity, Component>`, 태그 Component는 `Set<Entity>`에 저장한다.
- 중앙 ECS 클래스 이름은 `World`로 사용한다.
- 모든 System은 클래스로 작성하고 필요한 Component Store를 직접 순회한다.
- Entity 종류는 상속 클래스가 아니라 prefab 함수의 Component 조합으로 표현한다.
- System은 Input → AI → Movement → Collision → Rules → Audio/Effects → Render → Cleanup 순서로 실행한다.
- Canvas, AudioContext, 화면 상태, 원시 입력과 정적 레벨 데이터는 ECS 밖에 둔다.
- ZIP 예산이 위험하면 측정값과 제거한 경계를 기록한 뒤 필요한 ECS 부분만 평탄화한다.

## 선택한 콘셉트와 범위

- 테마 해석: 흩어진 프리즘 조각으로 무지개 포털을 복원한다.
- 플레이어 행동: 유니콘을 사방으로 움직여 프리즘 조각을 탐색·수집하고 적을 회피하거나 능력으로 제압한다.
- 25~40초 핵심 반복 구조: 큰 사각 맵을 탐색 → 프리즘 발견 → 수집과 진행도 피드백 → 남은 영역 탐색 → 전부 수집하면 성장 선택 활성화.
- 세션 목표: 10개 스테이지, 약 5~7분. 맵은 화면보다 훨씬 크며 카메라는 유니콘을 추적하고 모든 화면비에서 빈 공간 없이 월드를 표시한다.
- 입력: 이동은 방향키·WASD 또는 고정 속도 터치 조이스틱, 무지개 뿔 공격은 플레이 중 Space 또는 오른쪽 터치 버튼, 능력 선택은 클릭·터치 또는 상하좌우 방향키, 확정은 Space 또는 같은 카드 두 번째 터치.
- 성공 조건: 현재 스테이지의 모든 프리즘 조각을 수집한다.
- 실패 조건: 보호막이 없는 상태에서 치명적인 피해를 받는다.
- 난이도 방향: 적 없는 학습 구간 → 추적 적 → 원거리 적 → 체력이 높은 대형 적.
- 성장 후보 9개: 프리즘 지도, 적 감지, Space로 쓰는 무지개 뿔, 바라보는 방향 자동 무지개 탄환, 런당 1회 보호막, 이동 속도 증가, 프리즘 자석, 감속 오라, 프리즘 수집 무지개 파동.
- 적 진행: Stage 1부터 추적 적 1마리를 두고 매 Stage 해금된 적 종류를 소폭 누적한다. Stage 4부터 짧은 사거리의 느린 투사체를 쏘며 계속 추격하는 원거리 적, Stage 7부터 느리고 체력이 높은 대형 적을 추가한다. 추적·원거리·대형 적 기본 속도는 각각 135·108·78이며 감속 오라 안에서는 35%로 감소한다.
- 정보 능력 조합: 미니맵만 있으면 유니콘과 프리즘, 적 감지만 있으면 검은 레이더에 유니콘과 적, 둘 다 있으면 통합 지도를 표시한다.
- 첫 playable 경계: 이동, 프리즘 수집, 단일 스테이지 완료, 단순한 능력 선택과 새로고침 없는 재시작.
- 명시적으로 제외할 범위: 최종 아트와 오디오, 다수 스테이지, 복잡한 스킬 조합, 보스, 이야기, 메뉴, 수작업 레벨 편집기.

## 보일러플레이트 정책

도구 설정에서 아이디어를 참고할 수 있지만 보일러플레이트 전체를 복사하지 않는다. 엔진, 사용하지 않는 추상화, 자산 파이프라인, 예제 게임플레이 코드를 피한다.

## 개발 단계

1. 규칙 수집
2. 테마 방향 상담
3. 프로젝트 초기화
4. 첫 플레이 가능 프로토타입
5. 핵심 반복 구조 조정
6. 레벨/콘텐츠 작업
7. 절차적 오디오 및 시각 요소 다듬기
8. 크기 최적화
9. 제출 패키징

## 검증 목록

- TypeScript 타입 검사가 성공한다.
- 디버그 빌드가 성공한다.
- 배포 빌드가 성공한다.
- ZIP 빌드가 성공하고 바이트 크기가 출력된다.
- 빌드된 HTML이 네트워크 접근과 콘솔 오류 없이 시작된다.
- 입력이 없는 첫 프레임과 몇 초 후의 위치, 속도, 시간, 점수가 모두 유한한 숫자다.
- 플레이어, 위험 요소, 점수 또는 진행 상태가 화면에서 서로 구분된다.
- 각 입력이 의도한 상태와 화면 변화를 만든다.
- 성공/실패 조건과 새로고침 없는 재시작을 실제로 확인한다.
- 화면 크기 변경 후 플레이어와 필수 UI가 화면 안에 남는다.
- 디버그 정보가 점수와 구분되며 배포 결과에서는 제거된다.
- 게임플레이 변경 사항을 현재 반복 구조에서 플레이테스트한다. 실행하지 못한 항목은 통과로 표시하지 않는다.
