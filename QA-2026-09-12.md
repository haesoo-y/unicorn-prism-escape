# 수정 및 회귀 검사 — 2026-09-12

## 적용 내용

- 600px 미만에서 타이머를 둘째 줄에 배치하고 프리즘 표시와 미니맵을 28px 내렸다. 게이트 생성 안내는 GATE OPEN으로 축약했다. 320px뿐 아니라 긴 게이트 안내가 겹칠 수 있는 중간 너비도 처리했다.
- 스킬 상하 이동은 목적 행의 선택 가능한 스킬로 이동한다. 모두 획득한 행은 건너뛰며, 선택이 실제로 바뀔 때만 효과음을 발생시킨다. 좌우 순환과 두 번 터치 확정을 유지했다.
- 문서의 적 속도, 게이트 생성 조건, 5분 목표를 현행 요구사항에 맞췄다. 5분은 실측 완료 수치가 아닌 목표다.
- 캐릭터와 배경·오디오 에셋에는 변경이 없다.

## 검증

- 가능한 강화 단계 64개 조합에서 선택 가능한 카드별 8개 방향키/WASD 입력: 회차당 1,152건, 2회 통과.
- GATE MAP 획득 후 PRISM MAP에서 아래 입력 시 RAINBOW HORN 이동: 2회 통과.
- 같은 카드 두 번 터치 확정, 이동할 수 없는 상태의 효과음 억제: 2회 통과.
- 실제 RenderSystem의 HUD 텍스트 출력 좌표 검사: 320, 360, 390, 599, 600, 844, 960px에서 게이트 전후 총 14개 경우, 2회 통과. 18px monospace의 논리적 글자 폭에 근거하며 실제 브라우저 픽셀 검사는 아니다.
- strict TypeScript, debug, production, ZIP: 2회 실행.
- ZIP 기준 12,812 bytes → 12,887 bytes (+75), 제한 13,312 bytes 대비 425 bytes 여유.

재실행:

```sh
./node_modules/.bin/esbuild tools/regression-check.ts --bundle --platform=node --format=esm --loader:.webp=dataurl --define:DEBUG=false --outfile=/tmp/upe-regression.mjs
node /tmp/upe-regression.mjs
npm run debug
npm run zip
npm run size
```

## 남은 검증 및 판정

코드 회귀 검사 통과, 실제 화면·플레이 검증 미완료.

이번 실행 환경의 브라우저 도구가 http://localhost:8080 접속을 ERR_BLOCKED_BY_CLIENT로 차단했다. 따라서 수정된 UI의 실제 픽셀, Chrome/Firefox 각각의 배포본 실행, 실제 모바일 터치, 10스테이지 두 번 완주와 5분 난이도 검증은 통과로 판정하지 않는다. 접속 가능한 브라우저 환경에서 이 항목들을 이어서 검사해야 한다. 로컬 서버는 실행해 둔다.

사용한 검사 지침: js13k-playtest-reviewer.

## 추가 수정 — 유니콘·음악·증원·속도

### 사용자 요청과 적용

- 유니콘 PNG와 WebP의 보이는 픽셀을 비교해 차이가 0임을 확인했다. 압축 손상으로 보지 않는다. 기존 48×48·모든 방향·무지개 갈기·작은 검은 점 눈과 에셋 파일은 유지했다.
- 유니콘의 소각도 회전을 제거하고 몸통/다리 소스 영역의 중복 절단을 없앴다. 8방향 모두 다리 두 그룹을 교대로 움직이며, 옆면 꼬리 영역은 별도로 유지한다. 중립 보행 위상은 온전한 원본 스프라이트와 픽셀 단위로 같다. 큰 원본 재제작·다운샘플링은 적용하지 않았다.
- 음악은 기존 Web Audio 시퀀서의 저음·화음·리드·타악을 보강했다. 배경 1–2 / 3–5 / 6–8 / 9–10에 맞춰 132 / 144 / 156 / 168 BPM으로 바뀐다. 종료된 oscillator/gain을 disconnect하며 기존 입력 잠금 해제·M 음소거를 유지한다.
- 플레이 시간 5초마다 해금된 종류별 1마리씩 서로 다른 맵 변에서 증원한다. Stage 1–3은 근거리, 4–6은 근거리+원거리, 7–10은 세 종류다. 중심은 경계에서 48px 안쪽이며 플레이어와 160px 이상 떨어진다. 강화·실패·완료에서는 증원하지 않으며 재시작/스테이지 이동 때 카운터를 초기화한다.
- 적 속도 = Stage 1 기준 200/180/160 + (표시 스테이지−1)×10 px/s. 원거리 Stage 4=210, 대형 Stage 7=220, Stage 10=290/270/250. 감속 오라는 증가 후 속도의 35%. 적 탄환은 속도 300px/s·수명 2초 유지.
- 대시보드의 속도·증원 계산을 갱신하고 원본 크기 화면에 제자리 보행 및 선택 스테이지 음악 비교 버튼을 추가했다. 생성기는 `node tools/build-dashboard.mjs`이며 대시보드는 배포 ZIP에서 제외된다.

### 실행 검증

- check/debug/zip/size 각각 최종 코드로 2회 성공. zip 명령 안의 production 빌드도 각 회 성공.
- 기존 회귀: 회차당 방향 입력 1,152건, HUD 논리 좌표 14경우, 두 번 터치 확정 — 2회 통과.
- 신규 회귀: 회차당 10스테이지×32개 결정적 난수 시나리오에서 5/10/15초 증원·같은 회차의 변 중복 없음·플레이어 안전 거리·5초 경계 중복 방지·비플레이 정지 — 2회 통과.
- 속도: 10스테이지×3종류×감속 유무 60경우 — 2회 통과. 대형의 기존 가속/방향 전환 보간이 수렴한 속도를 비교했다.
- 실제 Game 클래스의 프레임 실행에서 10스테이지의 강화 중 타이머 정지·재시작 증원 카운터 초기화·초기 적 수 복원·누적 시간 보존·전체 재시작 시간 초기화 — 2회 통과.
- 그리기 호출 검사: 8방향×4위상에서 회전 없음·정수 위치·원본/출력 크기 동일 — 2회 통과.
- 별도 native Canvas(@napi-rs/canvas) 실행: 각 8방향에서 중립 보행 픽셀=온전한 원본, 서로 반대인 다리 위상의 픽셀 차이, Canvas transform 복원 — 2회 통과. 32개 방향/위상 표를 `/tmp/upe-unicorn-motion.png`로 렌더링해 시각 확인했다. 브라우저 렌더링은 아니다.
- 오디오 API 대역 실행: 입력 전 미생성·입력 반복 시 컨텍스트 1개·M 음소거·종료 노드 해제 — 2회 통과. 32스텝 음표 수는 배경별 120/148/196/204, 스텝 간격은 약 0.227/0.208/0.192/0.179초. 실제 음악 청음은 아니다.
- 대시보드 Node VM 상태 검사: 회차당 3프리셋×10스테이지, 순차 강화 편집 9번, 독립적인 동적 계획법과 수집 경로 비교, 시간·증원 가정 변경 — 2회 통과. DOM 대역 검사이며 실제 클릭 검사가 아니다.
- 기존 localhost:8080 서버의 HTTP 응답이 갱신된 debug HTML과 바이트 단위로 같음을 확인했다. 새 서버 시작은 기존 포트 사용으로 EADDRINUSE였으므로 기존 서버를 종료하거나 교체하지 않았다.

재실행:

```sh
npm run check
npm run debug
npm run zip
npm run size
./node_modules/.bin/esbuild tools/change-check.ts --bundle --platform=node --format=esm --loader:.webp=dataurl --define:DEBUG=false --outfile=/tmp/upe-change-check.mjs
node /tmp/upe-change-check.mjs
./node_modules/.bin/esbuild tools/regression-check.ts --bundle --platform=node --format=esm --loader:.webp=dataurl --define:DEBUG=false --outfile=/tmp/upe-regression.mjs
node /tmp/upe-regression.mjs
node tools/build-dashboard.mjs
```

### ZIP 측정

| 단계 | ZIP bytes | 직전 대비 |
| --- | ---: | ---: |
| 변경 전 재빌드 | 12,887 | — |
| 유니콘 렌더링 | 12,893 | +6 |
| 음악 편곡 | 12,900 | +7 |
| 증원·속도·초기화 | 13,063 | +163 |
| 오디오 종료 노드 정리 | 13,077 | +14 |

총 +190바이트, 제한 13,312바이트 대비 235바이트 여유. 최종 2회 측정값 동일.

### 미검증과 다음 확인

실제 게임 http://localhost:8080/ 접속을 다시 시도했지만 브라우저 도구가 ERR_BLOCKED_BY_CLIENT로 차단했다. 브라우저별 실제 픽셀·입력·음악 청음·모바일 터치·완주 난이도는 미검증이다. 기존 에셋의 픽셀 보존과 움직임 프레임 차이가 사용자의 시각적 만족이나 자연스러운 보행을 보장하지 않는다. 대시보드에서 전후 인상을 확인한 뒤 필요하면 추가 조정한다.

후반 근거리 목표 속도 290px/s는 속도 강화 플레이어 288px/s보다 높다. 지속 증원으로 적이 누적되는 요청을 그대로 구현했으며 개체 수의 임의 상한을 추가하지 않았다. 후반 회피 가능성·장시간 프레임 성능·약 5분 목표는 실제 완주로 판단해야 한다. 대시보드의 시간 모델은 이 적 압박을 별도로 수치화하지 않는다.

## 난이도 완화 및 4방향 탄환 — a113256 이후

이 절은 위의 5초 무제한 증원·스테이지당 +10px/s 기록을 대체하는 최신 동작이다.

- 적 속도 증가를 스테이지당 +5px/s로 완화. Stage 10 근거리/원거리/대형은 245/225/205px/s.
- 증원 주기 8초, 상한은 초기 적 수 + 해금 종류 수 × 3. Stage 1–10은 4/7/9/15/17/19/24/27/30/35마리.
- 생존 적만 상한 계산에 포함한다. 같은 프레임에 소비 표시된 적은 제외한다. 상한에서 지난 회차를 건너뛰고 다음 회차에 부족한 자리만 보충한다. 각 회차에는 종류당 최대 1마리, 부족한 자리는 마지막으로 추가한 종류 다음부터 순환한다. 순환 인덱스도 스테이지 시작/재시작에 초기화한다.
- RAINBOW BOLT는 바라보는 방향에 관계없이 유니콘에서 상하좌우 4발 동시 발사. 기존 간격 1초·속도 420px/s·수명 1초·피해량 유지. 적 탄환은 변경하지 않았다.
- tools/change-check.ts를 확장했다. 회차당 기존 증원 시나리오 320개·속도 60경우·10개 스테이지 프레임/재시작 검사에 더해, 10스테이지 상한/처치/종류 순환/밀린 증원 없음, 서로 다른 바라보기 3경우의 4방향 위치·속도·발사 간격·강화 중 정지를 검사했다. 2회 통과.
- 기존 스킬/HUD 회귀 검사와 check/debug/zip/size를 각각 2회 통과. ZIP은 13,077 → 13,148바이트(+71), 제한 대비 164바이트 여유. 최종 두 번 동일.
- 대시보드에 최신 속도·8초 주기·상한·4방향 스킬 설명 반영. Node VM 검사 2회 통과.
- 검증은 Node에서 실제 게임 소스를 실행한 상태/호출 검사이며 Canvas/Audio/DOM 대역을 사용한다. 이번 변경의 실제 브라우저 플레이·모바일·완주 난이도는 미검증이다. 앞서 기록한 브라우저 차단을 우회하지 않았다.

## 탄환·오라 조합 추가 조정

- 4방향 무지개 탄환 발사 간격을 2초로 변경했다. 속도 420px/s·수명 1초·4방향 동시 발사는 유지한다.
- 오라 안의 적 이동 속도는 해당 스테이지 원래 속도의 75%(25% 감속)로 변경했다. 반경 220px 및 적 탄환 35% 속도(105px/s)는 유지한다.
- 오라 테두리와 감속된 적/탄환의 외곽선을 #75fff044, 약 27% 불투명도로 변경했다. 기존 오라 내부 채움은 유지한다.
- 신규 회귀에서 2초 발사 경계, 오라 적 속도 60경우, 적 탄환의 오라 안/밖 105/300px/s, 오라 strokeStyle을 검사했다. 기존 증원·상한·순환·재시작·HUD·스킬 검사와 함께 2회 통과했다. Node API 대역 실행이다.
- 별도 native Canvas에서 오라를 투명 배경에 렌더링해 중심 alpha 24, 전체 최대 alpha 86로 반투명을 확인했다. 2회 통과. 브라우저 픽셀 검사는 아니다.
- check/debug/zip/size 각각 2회 통과. ZIP 13,148 → 13,147바이트(-1), 남은 예산 165바이트. 대시보드도 최신 수치로 갱신했다.
- 실제 브라우저 플레이와 조합 난이도 평가는 미검증. 커밋하지 않았다.

## 오라 투명도 추가 조정

- 오라·감속 표시 외곽선 #75fff044 → #75fff022: 불투명도 약 27% → 13%.
- 오라 내부 채움 #64ffe818 → #64ffe808: 불투명도 약 9% → 3%. 범위와 감속 성능은 그대로다.
- check/debug/zip/size 및 신규 회귀 검사 각각 2회 통과. 별도 native Canvas에서 중심 alpha 8, 최대 alpha 41 확인 — 2회 통과. 실제 브라우저 시각 검사는 미검증.
- ZIP 13,147 → 13,143바이트(-4), 여유 169바이트. 대시보드 재생성 완료. 커밋하지 않았다.

## 오라 감속·범위 재조정

- 적 이동 속도 75% → 65%(35% 감속), 효과 반경 220 → 200px. 적과 적 탄환의 범위 판정 및 감속 외곽선 판정을 함께 갱신했다. 오라 시각화의 기본 반경도 200px이며 기존 ±5px 맥동을 유지한다.
- 적 탄환 속도 비율 35%, 반투명 외곽선/내부 채움, 탄환 발사 간격 2초 유지.
- 199/200/201px 경계에서 적 속도와 적 탄환 속도, 시간 0의 표시 반경 200px를 실제 소스로 검사했다. 신규 회귀 및 check/debug/zip/size 각각 2회 통과. Node API 대역 검사이며 실제 브라우저 시각·플레이 검사는 미검증.
- ZIP은 13,143바이트로 동일하며 169바이트 여유. 대시보드 갱신, 커밋하지 않음.

## 대형적 접지·보행 및 유니콘 교체 시안

- 대형적 PNG의 실제 불투명 범위는 해당 96×96 셀 안에서 (1,18)–(94,76)이다. 기존 몸 전체 bob/rotate/scale이 발을 고정 그림자에서 들어 올렸다.
- 원본 96×96 에셋은 유지하고 대형적의 전체 수직 이동·회전·스케일 변형을 제거했다. 그림자 윗줄 y+38과 바닥에 둔 발의 마지막 불투명 줄 y+38을 맞췄다. 좌우 흔들림은 그림자도 함께 따른다.
- 다리 4개 영역은 두 그룹이 교대로 최대 8px 들리고 최대 3px 앞뒤로 움직인다. 다리를 먼저 그리고 상체를 덮어 이음매를 처리한다. 모든 이미지 소스/출력 영역 크기는 동일하며 정지 시 다리 위상은 0으로 돌아간다.
- 기존 및 신규 회귀를 합친 tools/change-check.ts 2회 통과. 대형적 32개 좌우/위상 호출 검사 포함. 별도 native Canvas에서도 32개 좌우/위상마다 바닥 발과 그림자 픽셀 접촉, transform 복원을 2회 확인했다. 30개의 서로 다른 보행 렌더 결과 확인. 실제 브라우저 검사는 아니다.
- check/debug/zip/size 각 2회 통과. ZIP 13,143 → 13,137바이트(-6), 여유 175바이트.
- 대시보드 캐릭터 보행 비교가 유니콘뿐 아니라 적에도 적용되도록 갱신했다.
- 유니콘은 ImageGen으로 기존의 흰 말·무지개 갈기/꼬리·작은 검은 점 눈·5개 기본 방향을 바탕으로 새 시안을 생성했다. 투명 배경 생성이 두 번 모두 실제 alpha 대신 체크무늬로 나와 게임에 사용하지 않았다. 마지막 시안은 하늘색 배경의 디자인 검토용 이미지다.
- 유니콘 원본 src/assets 파일과 유니콘 게임 렌더링은 이번 작업에서 바꾸지 않았다. /tmp/js13k-asset-preview-unicorn/comparison.png는 기존 48px 출력과 후보 48px 출력, 2.5배 관찰용 확대를 비교한다. 최종 투명 아틀라스 준비·게임 적용·ZIP 비용은 시안 승인 후 검증한다.
- 원본 시안: /Users/harry/.codex/generated_images/01a09582-b9b9-7db3-8e1a-a1bd69cc074f/exec-657651a3-92ff-4c2b-ae96-57de0f7d01d4.png
- 새 유니콘 시안 승인 대기. 커밋하지 않음.

## 승인된 통통한 얼굴 유니콘 적용

- 사용자가 둥근 볼/주둥이, 살짝 통통한 몸통, 콧구멍 없는 최종 시안을 승인해 적용했다. 승인 시안: /Users/harry/.codex/generated_images/01a09582-b9b9-7db3-8e1a-a1bd69cc074f/exec-53c42fcc-4e81-452f-a138-ff077ebf5351.png.
- 시안과 동일한 방향별 crop을 48×48 셀로 변환하고 하늘색 외부 배경을 투명화했다. 13색과 투명색으로 정리해 단단한 픽셀 경계를 유지했다. src/assets/unicorn-atlas.png 및 lossless WebP를 교체했다. 아틀라스는 240×48, 기본 5방향과 좌우 반전으로 8방향을 표시한다. 게임 렌더러의 48×48 표시와 기존 procedural 보행은 유지했다.
- PNG/WebP를 native Canvas로 각각 디코딩하여 픽셀 완전 일치, 5개 셀의 비어 있지 않은 실루엣, 0/255 alpha, 240×48 크기를 2회 실행 확인했다. 8방향×4위상 렌더 이미지도 확인했다. 방향별 중립 보행과 원본 픽셀 일치, 교대 다리 위상의 차이, transform 복원 검사를 2회 통과했다. 이는 native Canvas 실행이며 실제 브라우저 검사는 아니다.
- tools/change-check.ts 및 tools/regression-check.ts 각 2회 통과. 대시보드의 3프리셋×10스테이지 상태와 독립 DP 경로 대조도 Node VM/DOM 대역에서 2회 통과했다.
- npm run check/debug/zip/size 각각 2회 성공. 두 ZIP 모두 13,256바이트. 직전 13,137 대비 +119, 제한 13,312 대비 56바이트 여유. WebP 자체는 1,408 → 1,514바이트.
- stage-dashboard.html을 새 에셋과 ZIP 측정으로 재생성했다. 실제 브라우저 픽셀·Chrome/Firefox 배포본·모바일 터치·완주 난이도는 기존 접속 차단으로 여전히 미검증이다. 커밋·푸시는 수행하지 않았다.

## 정면 얼굴 재승인 시안 적용

- 사용자가 승인한 최신 시안 exec-ef6498cc-ec57-4269-bd21-c1bb469fed9d.png를 적용했다. 더 통통한 얼굴을 유지하면서 정면 관자놀이를 좁히고 둥근 턱으로 이어지도록 수정한 버전이다. 콧구멍 없는 얼굴·무지개 갈기/꼬리·작은 검은 점 눈을 유지한다.
- 기존 변환과 동일한 crop/48×48 출력/13색 팔레트를 사용하고 배경색 마스크의 허용 범위를 조정해 생성 이미지의 외부 배경 잔여 픽셀을 줄였다. 아틀라스 240×48, lossless WebP 1,486바이트. 렌더러와 게임 규칙은 이번 적용에서 변경하지 않았다.
- native Canvas에서 PNG/WebP 픽셀 일치·5개 48×48 셀·0/255 투명도, 8방향 중립 보행의 원본 일치·교대 다리 위상 차이·transform 복원 각각 2회 통과. 8방향×4위상 렌더 시각 확인. 실제 브라우저 검사는 아니다.
- tools/change-check.ts, tools/regression-check.ts, 대시보드 Node VM/DOM 대역 검사 각각 2회 통과. npm run check/debug/zip/size 각각 2회 성공.
- ZIP 두 번 모두 13,225바이트: 직전 13,256 대비 -31바이트, 제한 대비 87바이트 여유. 대시보드에 새 에셋과 측정값 반영.
- 실제 브라우저·모바일·완주 검증은 여전히 미검증이며 커밋/푸시는 수행하지 않았다.

## 승인된 부드러운 축소본 적용 및 배포 압축

- 승인한 원본의 면적 평균 축소본을 기준으로 48×48 셀을 유지했다. 큰 원본에서 픽셀 하나만 선택하던 변환 대신 면적과 alpha를 가중 평균하여 얇은 윤곽 손실을 줄였다. 새 캐릭터를 그리거나 외곽선을 덧칠하지 않았다.
- 배포용은 디더링 없는 적응형 16색 팔레트와 반투명 경계를 사용한다. 거의 투명한 잔여 픽셀을 제거하고 불투명 영역을 보정했다. PNG/WebP 모두 240×48, 반투명 픽셀 310개. WebP는 1,876바이트이며 원본 PNG와 raw RGBA 값이 완전히 일치하는지 libvips/sharp 디코딩으로 2회 확인했다.
- native Canvas의 PNG/WebP 디코딩 후 RGBA 비교는 반투명 픽셀의 디코더 반올림 차이 때문에 불일치했다. 이를 통과로 처리하지 않았으며, 위 raw 디코딩 결과와 구분한다. 게임용 WebP 자체로 8방향 보행을 실행해 중립 프레임과 원본 일치, 교대 위상 차이, transform 복원을 각 2회 통과했다.
- tools/build-game.mjs: 명시적으로 열거한 게임 내부 속성만 Terser로 축약한다(브라우저 built-in 예약은 기본대로 유지). ECS 구조와 게임 기능을 변경하지 않는다. 이미 압축된 WebP의 data URL은 Roadroller 입력에서 분리한 뒤 같은 배포 script에 다시 포함한다. 고정 Roadroller 파라미터도 측정으로 갱신했다. 외부 자원·런타임 의존성은 추가하지 않았다. HTML/CSS와 tools/build-zip.mjs는 기존 설정을 유지했다.
- 임시 비교 저장소는 tsconfig가 빠져 class field 변환 등이 실제 게임 빌드와 달랐다. 그곳에서 측정한 13,253바이트는 최종 값이 아니다. 실제 게임 저장소의 최종 ZIP은 두 번 모두 12,969바이트. 직전 실제 측정 13,225 대비 -256바이트, 제한 대비 343바이트 여유.
- npm run check/debug/prod/zip/size 각각 2회 성공. 원본 회귀 검사와 배포 속성 축약을 적용한 회귀 검사도 각각 내부 2회 통과. 대시보드 Node VM 상태 검사 2회 통과.
- tools/production-smoke.mjs: 실제 build/debug.html 및 build/index.html의 script를 Node VM과 Canvas/Image 대역에서 실행했다. 타이틀, Space 시작, 600프레임을 각각 2회 통과. 최초 5초 VM 제한에서는 배포 decoder가 시간 초과했으며 45초 제한으로 재실행해 통과했다. 브라우저 로딩 시간 측정은 아니다.
- 재실행: node tools/production-regression.mjs, node tools/production-smoke.mjs. 기존 tools/change-check.ts 및 tools/regression-check.ts 명령도 유지한다.
- 대시보드에 최신 에셋/크기를 반영했다. 실제 Chrome/Firefox 화면·모바일 터치·10스테이지 완주는 미검증이며 커밋/푸시는 하지 않았다.

## 최종 배포 점검 — df0f153 기준

판정: **배포 검사 완료, 실제 플레이 검증 미완료**. 검사 중 새로 확인한 게임 결함은 없다. 이는 실제 플레이에서 결함이 없다는 판정이 아니다.

### 이번에 실제 실행한 검사

- check/debug/prod/zip/size를 각각 2회 실행했다. 두 ZIP 모두 12,969바이트, 제한 대비 343바이트 여유.
- node tools/production-regression.mjs: 속성 축약을 적용한 게임 로직·입력·HUD·증원·오라·공격·재시작 검사 내부 2회 통과. Node와 API 대역 실행이다.
- node tools/package-check.mjs: ZIP 압축 해제, CRC, index.html 단일 엔트리, 배포 HTML과 바이트 일치, 내장 WebP 3개와 원본 파일의 바이트 일치, 외부 script/stylesheet 태그 없음, sourceMappingURL 없음 — 2회 통과. 네트워크 요청 관찰 검사는 아니다.
- 별도 libvips 이미지 디코딩: 내장 WebP 3개가 각각 240×48 / 192×96 / 80×88 크기로 디코딩됨 — 2회 확인. 적 아틀라스는 혼합 셀이 들어 있는 전체 크기이며 캐릭터 출력 크기와 다르다.
- node tools/production-smoke.mjs: 실제 debug/index HTML의 타이틀·Space 시작·600프레임 실행, 디버그 상태가 debug에만 존재하고 production에서는 노출되지 않음 — 각각 2회 통과. Canvas/Image 대역을 사용하는 Node VM이며 실제 브라우저 플레이가 아니다.
- 개발 서버 http://localhost:8080/ HTTP 200 확인. 이 서버는 모든 경로에 debug.html을 반환하므로 /index.html 주소를 입력해도 배포본 검사가 되지 않는다.

### 실행할 수 없었던 검사

브라우저 도구로 http://localhost:8080/ 페이지를 새로 열었지만 다시 net::ERR_BLOCKED_BY_CLIENT가 발생했다. 정책을 변경하거나 우회 경로를 사용하지 않았다. 다음 항목은 모두 미검증이다.

- 최신 유니콘의 실제 크기 선명도·8방향 보행과 대형적 발/그림자 접지.
- 작은 화면 HUD, 스킬 상하 이동, 실제 모바일 터치 입력/가로세로 전환.
- Chrome/Firefox 각각 배포본 시작·프리즘 수집·게이트·성장·실패·재시작, 실제 콘솔과 외부 요청.
- 10스테이지 두 번 완주, 약 5분 목표, 후반 증원/속도와 공격·오라 조합의 난이도.
- 배경 구간별 음악 변화와 효과음의 실제 청음.

### 실기기에서 이어갈 순서

1. build/game.zip을 별도 폴더에 풀고 그 안의 index.html을 Chrome과 Firefox에서 각각 연다. 개발 서버의 경로만 바꿔 배포본으로 간주하지 않는다.
2. 정지/8방향 이동을 보고 유니콘 윤곽·다리, 대형적 접지를 확인한다. 화면 너비 320/390/960px에서 HUD·미니맵·성장 화면을 확인한다.
3. 공격 우선 및 능력 우선 경로로 각각 10스테이지를 완주한다. 게임 누적 타이머로 시간을 기록한다. 강화 선택 시간은 제외한다. 실패했다면 스테이지·현재 스킬·실패 원인도 기록한다. 무적이나 적 제거를 사용한 검사는 난이도 검증으로 기록하지 않는다.
4. 음악 잠금 해제, 배경 구간별 변화, M 음소거, 실패 후 재시작을 확인한다.
5. 실제 모바일에서 이동+공격 동시 터치, 손을 뗀 뒤 정지, 카드 선택·확정, 회전 후 표시/터치 위치를 확인한다.

| 회차 | 기기 / 브라우저 | 스킬 획득 순서 | 도달 스테이지 | 누적 시간 | 실패/불편 및 재현 |
| --- | --- | --- | --- | --- | --- |
| 1 | 미실행 | 공격 우선 | — | — | — |
| 2 | 미실행 | 능력 우선 | — | — | — |

자동 검사만으로 난이도를 임의 조정하지 않았다. 현재 플레이 검증이 막힌 상태이므로 제출 완료로 처리하지 않았으며, 이번 검사 도구/문서 변경은 아직 커밋·푸시하지 않았다.

## 오프닝 픽셀 글꼴·엔딩 기록 — 7d30dc1 이후

- 사용자 승인 시안을 기준으로 오프닝 타이틀·수집 목표·조작 안내에 자체 픽셀 글꼴을 적용했다. 엔딩은 YOU ESCAPED 제목, 총 기록, STAGE 1–10 개별 기록, 재시작 안내로 구성했다. 예산에 맞춰 기존 밤하늘 배경을 재사용하고 축하 문구를 WELL DONE으로 간결하게 정리했다. 번호와 분 앞의 불필요한 0은 생략한다.
- 기존 캐릭터 에셋·출력 크기·보행, 오디오, 난이도는 변경하지 않았다. 게임 중 HUD와 스킬 화면 글꼴은 유지한다. 타이머 표시는 60분 이후에도 분을 누적한다.
- stageTimes는 게이트 진입 상태 전환 시 한 번 기록한다. 스테이지 재시작으로 초기화되는 stageElapsed 대신 전체 elapsed에서 앞선 기록 합을 빼므로 실패한 시도의 플레이 시간도 해당 스테이지에 포함된다. 성장 선택/실패 화면/완료 화면의 대기 시간은 제외된다. R로 전체 재시작하면 기록이 초기화된다.
- tools/screens-check.ts 추가: 10개 스테이지 기록·합계 보존·재도전 포함·중복 게이트 억제·선택/완료 중 시간 정지·전체 재시작·59.9996초 및 60분 포맷 경계를 2회 통과. 게이트 사건을 주입한 Node/API 대역 검사이며 실제 10스테이지 완주가 아니다.
- 320×480, 360×640, 720×540, 568×320에서 제목과 10개 기록의 출력 좌표를 검사해 총 8개 화면 경우를 2회 통과했다. 별도 native Canvas로 실제 RenderSystem의 8개 화면을 두 번 렌더링하고 확인했다. 작은 가로 화면은 유니콘을 게이트 옆에 두고 제목/기록 행을 줄여 겹침을 방지했다. 실제 브라우저 픽셀/터치 검사는 미검증이다.
- 기존 및 화면 회귀를 production-regression.mjs에서 최종 속성 축약 설정으로 각각 2회 통과했다. production-smoke.mjs는 픽셀 글꼴로 바뀐 타이틀의 사각형 출력과 Space 시작을 검사하도록 변경했다. debug/production 각각 600프레임, production 디버그 상태 미노출을 두 번 확인했다. VM/API 대역 실행이다.
- 빌드 압축: TypeScript에서 실제 표준 API 접근을 확인해 clear/height/next/state/type/value/width 및 DOMRect 구조 분해의 x/y를 축약에서 제외했다. 명시적으로 허용한 내부 속성에만 builtins 옵션을 적용했다. 기존 색인화 에셋 데이터는 변경하지 않았다. Roadroller 고정 파라미터를 재측정했고 ZIP 반복 탐색은 1,000회로 늘렸다. 배포 HTML의 생략 가능한 html/head/body 태그·안전한 속성 따옴표와 현재 border/padding이 없는 Canvas 문서에서 불필요한 box-sizing 기본 규칙을 제거한다. debug HTML은 기존 구조를 유지한다.
- check/debug/zip/size는 최종 코드로 각각 2회 성공. zip 내부 production 빌드 포함. 최종 두 ZIP 모두 13,309바이트, 직전 12,969 대비 +340바이트, 제한 대비 3바이트 여유. package-check.mjs의 CRC·단일 엔트리·내장 에셋 바이트 일치도 2회 통과했다.
- 대시보드에 오프닝/엔딩 버튼 추가. 엔딩 버튼은 명시적으로 예시 기록을 표시하며 실제 플레이 기록이 아니다. 기존 대시보드의 3프리셋×10스테이지 및 경로 계산 Node VM 검사는 2회 통과했다. 새 버튼의 실제 브라우저 클릭은 미검증이다.
- 재실행: node tools/production-regression.mjs, node tools/package-check.mjs, node tools/production-smoke.mjs. 브라우저 도구의 기존 ERR_BLOCKED_BY_CLIENT 차단을 우회하지 않았다. 실제 Chrome/Firefox·모바일·완주 검증은 여전히 남아 있다.
- 커밋/푸시는 하지 않았다.

## 레인보우 웨이브 — 2026-09-13

사용자 요청으로 프리즘 색 파동과 범위 확대를 적용했다. Event Flash / Outline Pulse: 수집 즉시 판정·시각 효과 시작, Major 시각 신호 및 기존 수집음 보조. 반경 190→300px, 밀치기 120px 유지, 데미지 없음. 파동은 24→300px 원형 선(6px), 알파 1→0, 600ms, 최대 7개다. 화면 섬광·흔들림·입력 잠금 없음. 프리즘 Entity의 위치·색 컴포넌트를 재사용하고 waves 수명 데이터로 중복 수집을 막는다. 수명은 플레이 중만 감소하고 만료 시 Cleanup, 재시작 시 World.clear로 제거한다. Canvas save/restore 안에서 효과를 그린다.

- 변경 전 실제 ZIP 13,309바이트. 최종 check/debug/zip/size 각 2회 성공, ZIP 안의 production 빌드 2회 성공. 최종 13,311바이트(+2), 제한 13,312 대비 1바이트 여유.
- 추가 직후 ZIP 13,414바이트로 초과했으나, 중복 참조·삭제 예약 제거, 내부 필드 이름 분리, 간결한 스킬 안내, Roadroller 설정 조정과 배포 HTML의 중복 선언 제거로 해결했다. ASCII 전용 배포 HTML에서만 charset 선언을 생략한다. ECS 경계와 원본 자산은 보존했다.
- tools/wave-check.ts: 7색 각각의 수집/파동 생성, 189/250/299/300/301px 경계, 적 밀치기, 적 탄환 제거와 아군 탄환 보존, 중복 수집 방지, 원점 고정, 600ms 만료, 비플레이 정지, 동시 7개와 clear 정리 — 2회 통과.
- production-regression: 위 검사 및 기존 입력/HUD/증원/속도/타이머/엔딩 검사를 배포와 같은 minify 설정으로 각 2회 통과.
- package-check: ZIP CRC, 단일 index.html, 배포 HTML 일치, 3개 WebP 원본 바이트 일치 — 2회 통과.
- 별도 native Canvas: 실제 RenderSystem을 360×640 및 960×720에서 발동 직후/200ms/400ms로 렌더링, 각 2회 실행. 프리즘 색의 원이 확장·감쇠하는 모습은 내부 시각 확인이며 실제 브라우저 검증이 아니다.
- 브라우저 http://localhost:8080/ 접속은 이번에도 ERR_BLOCKED_BY_CLIENT로 차단됐다. 실제 브라우저 파동 가독성, Chrome/Firefox 배포본, 모바일 터치, 완주·난이도는 미검증이다.

재실행: npm run check && npm run debug && npm run zip && npm run size; node tools/production-regression.mjs; node tools/package-check.mjs; node tools/production-smoke.mjs.

- 최종 production-smoke: debug/production HTML의 실제 스크립트 디코드·600프레임·Space 시작·배포 디버그 상태 비노출을 Node VM에서 각 2회 통과했다. 브라우저 실행/성능 측정은 아니다.

## 엔딩 기록 단일 열 — 2026-09-13

- 사용자 요청에 따라 화면 비율과 관계없이 1~10스테이지 기록을 한 열로 표시한다. 총시간은 위쪽에 유지한다. 높이 480px 미만은 1배 픽셀 글꼴, 그 외에는 2배 글꼴로 표시하며 화면 높이에 따라 행 간격을 조정한다.
- screens-check는 320×480, 360×640, 720×540, 568×320, 1280×720, 1920×1080, 844×390, 960×320, 960×440, 960×460에서 기록 10개의 순서·동일 열 정렬·시간과 행 일치·행 사이 최소 여백·하단 안내와 겹침 없음을 2회 실행 검사했다. Canvas 호출 좌표 검사이며 브라우저 픽셀 검사는 아니다.
- 최종 check/debug/zip/size 각 2회 성공. ZIP 13,311→13,278바이트(-33), 여유 34바이트. production-regression 및 package-check 각 2회 통과.
- 별도 native Canvas 렌더링으로 720×540와 568×320 엔딩의 단일 열을 시각 확인했다. 대시보드도 최신 RenderSystem으로 다시 생성했다. 실제 브라우저 실행은 기존 접속 차단으로 미검증이다.

## 원거리 적 녹색 불꽃 — 2026-09-13

- 사용자 요청대로 원거리 적의 절차적 투사체 팔레트를 자주/적갈/주황색에서 어두운 녹색 4단계로 바꿨다. 외곽 #092d26, 몸체 #145c40/#258052, 중심 #64bd98. 보조 공격 자산이므로 별도 승인 단계 없이 기존 Canvas 경로와 3위상 꼬리 움직임을 활용했다. 적 캐릭터 및 유니콘의 에셋, 아군 투사체는 유지했다.
- 변경 직전 check/prod/zip/size로 13,278바이트를 측정했다. 최종 check/debug/zip/size 각 2회 성공, ZIP 13,284바이트(+6), 여유 28바이트.
- 별도 native Canvas에서 8방향×밝은/어두운 배경을 렌더링했다. 4색 픽셀 존재, 3위상 꼬리의 픽셀 변화, Canvas transform 복원을 각각 2회 확인했다. 실제 화면 크기에서 적과 불꽃을 나란히 시각 검토했다. 결과 build/green-fire-review.png.
- 기존 production-regression 및 package-check 각 2회 통과. 적 탄환 속도 300px/s·수명 2초, 감속 105px/s 등 기존 동작 검사를 포함한다.
- 실제 브라우저 플레이는 기존 ERR_BLOCKED_BY_CLIENT 환경 제한으로 미검증이다. native Canvas 결과를 브라우저 실행으로 간주하지 않는다.

## 원거리 불꽃 완전 재제작 — 2026-09-13

- 사용자가 앞선 색상 변경 결과를 거절하여 적 투사체의 기존 각진 경로와 사각형 중심을 제거했다. 새 quadraticCurveTo 경로로 세 갈래 휘어진 불길을 만들고 크기·위치가 다른 3겹으로 렌더링한다. 외곽 #103d2f, 중간 #287f50, 속불 #8bdb9b. 불길 끝은 3위상으로 일렁인다. 팔레트는 정적 상수로 공유한다. 아군 투사체와 적의 속도·수명·판정은 변경하지 않았다.
- 최종 check/debug/zip/size 각 2회 성공. ZIP 13,284→13,308바이트(+24), 여유 4바이트. production-regression/package-check 각 2회 통과.
- 실제 RenderSystem의 native Canvas 실행에서 8방향×2배경, 새 팔레트 3색의 픽셀 존재, 3위상 애니메이션 변화, transform 복원 검사를 각각 2회 통과했다. build/green-fire-review.png를 새 결과로 갱신했다.
- 실제 브라우저 플레이는 미검증이며 native Canvas 이미지를 브라우저 결과로 보고하지 않는다.

## 불꽃 청록색 보정 — 2026-09-13

- 사용자 피드백에 따라 새 불꽃의 형태를 유지하고 팔레트를 원거리 적의 청록색에 맞췄다: 외곽 #073344, 중간 #087f9d, 속불 #66eeff. 앞선 녹색 팔레트는 폐기했다.
- check/debug/zip/size 및 package-check 각각 2회 성공. ZIP 13,308→13,310바이트(+2), 여유 2바이트.
- native Canvas에서 8방향×2배경, 청록색 3단계 픽셀 존재, 불길 3위상 변화, transform 복원을 각 2회 확인했다. 결과 build/teal-fire-review.png. 실제 브라우저 플레이는 미검증이다.

## 불꽃 크기 축소 — 2026-09-13

- 각 불꽃 층의 배율을 i/3에서 i×0.3으로 바꿔 10% 축소했다. 속불 중심 위치, 청록 팔레트, 적 투사체 판정과 동작은 유지한다. 프로젝트에서 변경되지 않는 Canvas 기본 lineJoin=miter/lineCap=butt 중복 지정을 제거했다.
- 최종 check/debug/zip/size, production-regression, package-check 각 2회 통과. ZIP 13,310→13,290바이트(-20), 여유 22바이트.
- native Canvas 8방향×2배경, 3색 팔레트, 3위상 변화, transform 복원 각 2회 통과. 축소 결과를 시각 확인하고 build/teal-fire-review.png를 갱신했다. 실제 브라우저 플레이는 미검증이다.

## 불꽃 추가 축소 — 2026-09-13

- 직전 90% 버전에서 불길 크기와 층의 상대 위치를 80%로 줄였다. 각 층은 최초 대비 72% 크기다. 색·형태·애니메이션·충돌 판정은 유지한다.
- check/debug/zip/size 및 package-check 각 2회 통과. ZIP 13,290→13,294바이트(+4), 여유 18바이트.
- native Canvas 8방향×2배경, 3색 팔레트, 3위상 변화, transform 복원 각 2회 통과. 축소 화면을 시각 확인하고 build/teal-fire-review.png를 갱신했다. 실제 브라우저 검증은 미완료다.

## 불꽃 미세 축소 — 2026-09-13

- 직전보다 10% 더 축소해 최초 대비 64.8%로 표시한다. 각 층 배율 i×0.216, 상대 위치 (3−i)×2.16. 색상·형태·투사체 판정은 유지한다.
- check/debug/zip/size 및 package-check 각 2회 통과. ZIP 13,294→13,296바이트(+2), 여유 16바이트.
- native Canvas 8방향×2배경, 3색, 3위상 변화, transform 복원을 각 2회 검사하고 이미지를 시각 확인했다. build/teal-fire-review.png 갱신. 실제 브라우저 검증은 미완료다.

## 새 청록 원거리 악마 적용 — 2026-09-13

- 사용자가 승인한 3차 시안(작은 뿔·송곳니 두 개·손 위 불꽃)을 enemy-atlas.png/webp의 48×48 원거리 셀에 적용했다. 큰 원화를 44px 영역으로 축소·중앙 배치한 뒤 짙은 청록/청록/밝은 청록/아이보리의 4색과 반투명 경계로 정리했다. 부드러운 경계의 중간색을 줄인 최종 그림을 native Canvas에서 확인했다.
- 원거리 적만 (48,0,48,35)+(48,35,48,13)으로 분할하여 기존 중복 5px를 제거했다. 회전·좌우 흔들림 대신 정수 좌표 상하 보행을 사용한다. 캐릭터 주변의 기존 작은 궤도 입자를 없애고 새 손 위 불꽃과 해당 위치의 발사 예고를 사용한다. 이동 속도·발사 주기·투사체 판정은 유지했다.
- 새 적 WebP 1,660→1,708바이트(+48). 처음 16색 원화 적용 시 ZIP이 13,591바이트로 초과했으나, 팔레트 단순화·무손실 WebP 압축·겹침/불필요한 흔들림 정리로 해결했다. 테스트한 SVG 격자 저장 방식과 압축 매개변수 변경은 채택하지 않았다.
- 최종 check/debug/zip/size 각각 2회 성공. ZIP 13,296→13,306바이트(+10), 여유 6바이트.
- production-regression 각 2회 통과: 새 원거리 적 16위상×좌우 2방향에서 회전 없음·정수 이동·48px 원본/출력 크기 일치·분할 겹침 없음 32경우를 추가했다. 기존 타이머/엔딩/웨이브/속도/투사체 검사를 유지했다.
- sharp raw RGBA 비교 각 2회: PNG/WebP 완전 일치, 원거리 셀 외의 일반/대형 적 픽셀 완전 보존. 변경된 원거리 셀 픽셀 1,158개.
- package-check 각 2회 통과. native Canvas에서는 유니콘 옆의 원거리 적 48×48 렌더링, 좌우 방향·보행·발사 예고를 밝은/밤 배경에서 시각 확인했다. 결과 build/ranged-demon-review.png. 실제 브라우저 플레이는 기존 접속 차단으로 미검증이며 native Canvas 확인과 구분한다.

## 승인된 악마 3종 통일 적용 — 2026-09-13

- 승인 시안: 보라색 근거리, 같은 형태의 청록 불꽃 원거리, 넓은 어깨·긴 팔의 회색 두 발 대형. 모두 입 없는 얼굴이며 기존 유니콘은 보존했다. 스프라이트 48/48/96px 크기 유지.
- 새 아틀라스 WebP 1,708→1,764바이트(+56). 처음 상세 팔레트 적용은 ZIP 13,442바이트로 초과했다. 일반/원거리 3색, 대형 4색과 반투명 경계로 정리하고 공통 렌더링을 사용해 최종 13,306→13,245바이트(-61), 여유 67바이트. ECS 경계와 게임 규칙은 변경하지 않았다.
- check/debug/zip/size 각 2회 성공. production-regression 내부 각 2회: 악마 종류별 16위상×좌우 2방향, 원본 전체 영역의 겹침 없는 분할, 원본/출력 크기 일치, 정수 이동·무회전·교대 다리 들기를 검사했다. 기존 속도/증원/공격/타이머/엔딩/파동 검사도 통과했다.
- package-check 각 2회: ZIP CRC, 단일 HTML, 원본 에셋 포함 일치. 별도 PNG/WebP raw RGBA 완전 일치 및 192×96 아틀라스 크기 검사 2회 성공.
- production-smoke debug/prod 각각 600프레임, 2회 통과(Node VM). 실제 RenderSystem을 native Canvas에서 밝은/밤 배경, 3종×4자세로 2회 렌더링하고 시각 확인했다. build/enemy-family-review.png에 보관했다. 대시보드도 새 렌더러/에셋/ZIP 크기로 갱신했다.
- 이번 실행은 Node 및 native Canvas 검증이다. 실제 브라우저 실행·Chrome/Firefox·모바일 터치·완주는 미검증이며 이전 ERR_BLOCKED_BY_CLIENT 제한 이후 재확인하지 않았다.

## 제출 기준 보관·무손실 압축·모바일 입력 — 2026-09-13

- 기준 173b5be ZIP을 build/release-173b5be/game.zip에 보관했다. SHA-256: 8cf674216672a5d44235d157d23f836b15f919d7acfc8a92943b8e4f643d4f3c.
- Roadroller optimize(1)은 절감이 없어 채택하지 않았다. 적 WebP 무손실 압축을 q=100으로 변경해 1,764→1,714B. RGBA 픽셀 일치 2회 확인. ZIP 13,245→13,205B.
- 사용자 Chrome 실행 및 Chrome 모바일 모드 터치 이동 정상 확인. 실제 휴대폰·Firefox·완주·시간은 여전히 미확인.
- 실패/엔딩에서 탭 재시작을 추가했다. 실패 시 스테이지·능력·누적시간·완료 기록 유지, 엔딩 시 전체 초기화. R 키 지원 유지. 안내 TAP OR R TO RETRY / RESTART.
- 고정 조이스틱: 왼쪽 66px, 아래 66px, 외곽 반경 44px, 입력 반경 56px. 플레이 중 항상 표시. 터치/펜/마우스로 원을 누르거나 드래그하고, up/cancel/lostpointercapture 시 멈춤. 두 번째 포인터가 이동 포인터를 덮어쓰지 않음.
- check/debug/zip/size 각 2회 성공, 최종 ZIP 13,204B, 여유 108B. production-regression 내부 2회: 탭 실패 재시도/엔딩 초기화, 고정 조이스틱 3포인터 종류·두 번째 포인터·취소·캡처 해제·4화면 표시 좌표를 검사했다. Node 합성 입력이며 실제 터치 검사가 아님. package-check 2회 통과. 새 조이스틱/탭 재시작의 사용자 브라우저 확인은 남아 있음.

## 시작부터 보이는 게이트 — 2026-09-13

- 사용자 고정 조이스틱·탭 재시작 확인 완료(Chrome 모바일 모드 맥락, 실제 휴대폰/Firefox와 구분).
- startStage에서 게이트 1개 생성: Stage 1 중앙, 이후 기존 600px 플레이어 거리/140px 적 거리 후보 탐색. 플레이 중 위치 고정, 재시도 시 재배치. 7색 수집 후 입장·순차 강화·10스테이지 목표는 유지.
- 수집 전 회색(grayscale), 반짝임 없음. 수집 완료 후 무지개. HUD/미니맵/입장 판정은 게이트 존재 대신 수집 완료를 사용.
- ZIP 13,204→13,241B(+37), 여유 71B. check/debug/zip/size 각 2회 성공. production-regression, package-check, production-smoke 각 2회 성공(Node; 실제 브라우저 아님).
- 신규 검사: 10스테이지 게이트 1개·위치 고정, 0~6색 진입 금지, 마지막 색 동시 입장, 최종 완료, 재시도 잠금, 잠긴 게이트 위 피격 실패, 색상 필터 복원. 시작 Entity 수 검사는 게이트 1개를 반영.
- native Canvas 실제 RenderSystem으로 회색/무지개를 2회 렌더링하고 시각 확인(build/gate-lock-review.png). 실제 Chrome/Firefox 새 게이트와 완주 시간 변화는 미검증.
- 대시보드 생성기의 문자열 길이 오류를 수정하고 실제 startStage 게이트 배치 구문을 공유하여 2회 생성 성공.

## Chrome 브라우저 직접 확인 — 2026-09-13

- 사용자 Chrome 확장 연결·사이트 권한 변경 후 localhost 접근 성공. 기존 ERR_BLOCKED_BY_CLIENT 상태는 이번 Chrome 연결에서는 해소됐다.
- 실제 localhost:8080 디버그 게임: 시작, 0/7 회색 게이트 위 미완료, 고정 조이스틱 표시, 실패 후 클릭 재시작 확인. 읽은 콘솔 경고/오류 없음.
- 별도 build/browser-gate-qa.html fixture에서 실제 Game/Collision/Rules/RenderSystem 사용: 적 제거·무적·프리즘 좌표 이동으로 0색 입장 차단→7색 수집→무지개/HUD/미니맵 활성→upgrade 전환을 2회 확인. 이는 직접 플레이 완주가 아니다.
- 360×640 viewport에서 HUD·고정 조이스틱 표시와 실패 후 클릭 재시작 2회 확인. 마우스 클릭 검사이며 실제 터치 드래그 검사는 아님. 임시 viewport 복원, 검증 탭 닫음.
- 초기 fixture는 수동/자동 프레임 혼용으로 타이머가 음수가 되어 해당 결과를 폐기했다. 프레임 호출 중에만 자동 예약을 차단하도록 fixture를 수정한 후 정상 누적 시간으로 재검증. 게임 소스 변경 없음.
- Firefox/실제 휴대폰/10스테이지 완주/배포 압축본 Chrome 실행은 미검증. 이번 실제 브라우저는 디버그 게임 및 소스 기반 fixture 검사다.

## Firefox 사용자 확인 — 2026-09-13

- 사용자가 “파이어폭스 잘되는거 확인완료”라고 정상 동작을 보고했다. 이전 Firefox 미검증 표기는 이 사용자 확인으로 갱신한다. 에이전트 직접 실행 결과는 아니다.
- 확인한 빌드·기기·세부 조작·콘솔 검사·10스테이지 완주 범위는 명시되지 않았으므로 개별 통과로 확대하지 않는다. 실제 휴대폰 터치와 완주·시간 측정은 계속 별도 항목으로 유지한다.

## 용량 확보 — 2026-09-13

- Roadroller 기본값 최적화는 13,288B로 악화되어 폐기. 기존 파라미터에서 탐색한 sparseSelectors만 고정하여 13,241→13,236B(5B 절감, 76B 여유). 소스/에셋/게임 동작은 동일.
- check/debug/zip/size 각 2회, production-regression/package-check/production-smoke 내부 2회 성공. Node 검사이며 실제 브라우저 검증은 후속 최종 단계에서 수행. 추가 기능을 충분히 수용할 여유는 아니므로 단계마다 크기를 재측정한다.

## 56px 유니콘 — 2026-09-13

- 승인된 고해상도 원화에서 면적 평균으로 56px용 아틀라스(280×56)를 다시 제작했다. 48px 결과를 확대하지 않았다. 흰 몸·통통한 얼굴·점 눈·콧구멍 없음·7색 갈기와 꼬리를 유지하고 128 알파 경계를 사용한다.
- 원본/출력 56×56, 8방향 유지. 다리 분할 43px·측면 꼬리 제외 10px·좌우 분할 26/28px, 정수 이동 procedural animation 유지. 충돌 반경은 변경하지 않는다.
- ZIP 13,236→13,263B(+27), 여유 49B. check/debug/zip/size 각 2회, production-regression/package-check 내부 2회 성공. 실제 RenderSystem/native Canvas 8방향×3포즈를 2회 렌더링·시각 확인. Node VM 600프레임 검사도 2회 통과.
- Chrome 새로고침에서 ERR_BLOCKED_BY_CLIENT 재발. 수정본 실제 Chrome 확인은 미완료이며 사용자에게 권한 재확인을 요청했다.

## 배경 곡선·그라데이션 — 2026-09-13

- 4시간대 하늘은 월드 높이에 맞춘 4색 연속 그라데이션. 구름은 2색 타원 겹침으로 다시 그려 기존보다 약 1.5배 넓게 표현, 투명도 45%. 해는 지름 60px 원, 밤은 지름 44px 보름달. 별·이동 속도·팔레트·게임 판정은 유지.
- ZIP 13,263→13,281B(+18), 여유 31B. check/debug/zip/size 각 2회와 production-regression/package-check/production-smoke 내부 2회 성공. 실제 RenderSystem/native Canvas 4팔레트 렌더 2회 시각 확인. Chrome은 클라이언트 차단으로 미완료.

## 단계별 게이트 색 복원 검토 — 2026-09-13

- 최종 배경 반영 후 ZIP은 13,281B, 여유 31B다. 사용자 조건부 대안에 따라 추가 마스크/색상 상태 렌더링 대신 회색→7색 수집 후 무지개 2상태를 유지한다. 단계별 색 복원과 추가 장식 에셋은 보류한다. 새 게이트 디자인이나 게임 규칙 변경은 없다.
- 게이트 0~6색 잠금, 7색 입장, 재시도 초기화, 10스테이지 전환은 production-regression 내부 2회 성공. 추가 구현의 정확한 바이트 비용을 측정한 것은 아니므로 예상 절감/증가 수치를 제시하지 않는다.

## 최종 자동 검증 및 남은 실기기 검증 — 2026-09-13

- 게임 코드 기준 ff5fa44, 최종 ZIP 13,281 / 13,312B(31B 여유). SHA-256: 5433bfb8ea4414bef6a8f6fa32a6598eaf6df352356415caa962b92b00b28d27. build/release-2026-09-13-final에 ZIP과 HTML을 보관한다.
- check/debug/zip/size 각 2회 성공. production-regression은 실제 소스의 minify/property mangle 결과를 사용하며 내부 2회 성공. package-check는 ZIP CRC·단일 루트 index.html·포함 WebP 일치 검사 2회 성공. production-smoke는 debug/압축 prod의 Node VM 600프레임×2회 성공. Canvas/Audio 대역 검사이며 실제 Chrome 또는 청취 결과가 아니다.
- node tools/playthrough-simulation.mjs: 실제 Game/입력/AI/이동/충돌/순차 강화를 사용하는 보조 봇. 렌더/오디오만 생략하며 적 제거·순간이동·무적 수정은 없다. seed13: 232초·실패1회 완주, seed97: 271초·실패5회 완주, seed42: Stage3 실패10회로 중단. 3조건×2회 동일 결과. 약5분 목표와 초반 경로/봇 정책 민감성은 사람의 플레이로 확인해야 한다. 이 결과로 난이도 통과를 선언하거나 자동 조정하지 않았다.
- stage-dashboard 2회 생성 및 Node DOM 대역 검사 30프리셋/스테이지 조합·순차 강화·독립 경로 계산 대조 2회 성공. HTML 계획서/대시보드 ID 중복·앵커·JS 구문 검사 2회 성공. 실제 HTML 화면 검증과 구분한다.
- Chrome에서는 이번 새로고침 때 ERR_BLOCKED_BY_CLIENT 재발. 사용자에게 연결/사이트 권한 재확인을 요청한 상태. 기존 Chrome·Firefox 사용자 확인은 이전 버전 기록으로 유지한다. 이번 수정본 실제 브라우저·오프라인 실행·음악 청취는 미완료.
- 실제 휴대폰: 고정 조이스틱 드래그, 이동 중 공격, 손 떼기/취소, 실패/완료 탭 재시작이 미완료. 사람이 하는 10스테이지 완주·누적/스테이지 시간 대조와 약5분 체감 난이도도 미완료. 브라우저 접근과 사용자 기기 확인 없이는 완료로 처리하지 않는다.
- 제출 이미지 규격·대표 이미지·소스 push·제출은 이번 1~6 작업 이후 별도 후속 항목이다. 자동 커밋만 수행하며 push/제출하지 않았다.

## 5분 목표 및 대각선 보행 수정 — 2026-09-13

- 사용자 지시에 따라 전체 완주 목표는 약 5분(300초, 스킬 선택 중 정지)으로 변경했다. AGENTS/QA/README/HTML 계획서/대시보드 템플릿과 생성본의 목표를 통일했다. 대시보드 차이 계산도 300초 기준이다. 기존 232초·271초 봇 실측은 변경하지 않으며 사람 완주 검증은 아직 남아 있다.
- 대각선 앞/뒤 그림의 중앙 분할이 발을 가르는 문제를 수정: 앞 대각선 x21, 뒤 대각선 x25에서 분할하고 하단 시작은 y45. 대각선 다리는 가로 이동 없이 교대로 0~2px 들어 올려 몸체와의 연결을 유지한다. 나머지 방향·56px 크기·이미지·게임 판정은 보존했다.
- 재발 검사: 대각선 4방향×16위상에서 발이 하나의 조각에 포함되고, 가로 분리가 없으며, 원본 면적 전체를 중복 없이 그리는지 확인. production-regression 내부 2회 성공. Native Canvas 실제 RenderSystem 8방향×3포즈 2회 시각 확인.
- check/debug/zip/size 각 2회와 package-check/production-smoke 내부 2회 성공. 대시보드 2회 생성, 300초 기준 차이 표기(기본 모델 302.6초→+0:03)를 포함한 Node DOM 대역 검사 2회 성공. 저장소에서 이전 완주 목표 표기 잔존 없음; 대회 마감 시각은 보존.
- Chrome 새로고침은 여전히 ERR_BLOCKED_BY_CLIENT. 실제 브라우저 보행 검사는 미완료이며 네이티브 렌더 결과와 구분한다.
- ZIP 13,281→13,297B(+16), 여유 15B. SHA-256 289bff6ee7124a719f359b90834523297fc51311580163bd75d8a5e4909961f1. 이전 보관 ZIP은 이전 기준본으로 유지한다.

## 측면·후면 보행 잔여 선 수정 — 2026-09-13

- 후면 셀의 (53,43~45)에 고립된 3픽셀 세로선이 있었다. 이 3픽셀만 투명하게 정리했으며 PNG/WebP 가시 RGBA 일치와 정확히 3픽셀 변경을 2회 검사했다.
- 측면 고정 꼬리 영역 끝 x10이 뒷다리 윤곽 x9를 포함해 움직일 때 가는 선을 남겼다. 경계를 x8로 옮겨 뒷다리가 하나의 이동 조각에 포함되도록 수정했다. 56px 크기·방향·기존 다리 이동과 대각선 수정은 유지한다.
- 실제 RenderSystem/native Canvas에서 측면·후면 4위상을 확대 렌더링해 2회 확인. 측면 양방향×16위상의 고정 꼬리/다리 비중첩 검사를 추가했고 전체 production-regression 내부 2회 통과. check/debug/zip/size 각 2회, package-check/production-smoke 내부 2회 통과. 브라우저 직접 확인은 기존 클라이언트 차단으로 미완료다.
- ZIP 13,297→13,280B(17B 감소), 여유 32B. 대시보드와 HTML 검토 그림/현재 용량을 갱신했다.
