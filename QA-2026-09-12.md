# 수정 및 회귀 검사 — 2026-09-12

## 적용 내용

- 600px 미만에서 타이머를 둘째 줄에 배치하고 프리즘 표시와 미니맵을 28px 내렸다. 게이트 생성 안내는 GATE OPEN으로 축약했다. 320px뿐 아니라 긴 게이트 안내가 겹칠 수 있는 중간 너비도 처리했다.
- 스킬 상하 이동은 목적 행의 선택 가능한 스킬로 이동한다. 모두 획득한 행은 건너뛰며, 선택이 실제로 바뀔 때만 효과음을 발생시킨다. 좌우 순환과 두 번 터치 확정을 유지했다.
- 문서의 적 속도, 게이트 생성 조건, 3분 목표를 현행 요구사항에 맞췄다. 3분은 실측 완료 수치가 아닌 목표다.
- 캐릭터와 배경·오디오 에셋에는 변경이 없다.

## 검증

- 가능한 강화 단계 64개 조합에서 선택 가능한 카드별 8개 방향키/WASD 입력: 회차당 1,152건, 2회 통과.
- GATE MAP 획득 후 PRISM MAP에서 아래 입력 시 RAINBOW HORN 이동: 2회 통과.
- 같은 카드 두 번 터치 확정, 이동할 수 없는 상태의 효과음 억제: 2회 통과.
- 실제 RenderSystem의 HUD 텍스트 출력 좌표 검사: 320, 360, 390, 599, 600, 844, 960px에서 게이트 전후 총 14개 경우, 2회 통과. 18px monospace의 논리적 글자 폭에 근거하며 실제 브라우저 픽셀 검사는 아니다.
- strict TypeScript, debug, production, ZIP: 2회 실행.
- ZIP 기준 12,812 bytes → 12,887 bytes (+75), 제한 13,312 bytes 대비 425 bytes 여유.

재실행:

```sh
./node_modules/.bin/esbuild tools/regression-check.ts --bundle --platform=node --format=esm --loader:.webp=dataurl --define:DEBUG=false --outfile=/tmp/upe-regression.mjs
node /tmp/upe-regression.mjs
npm run debug
npm run zip
npm run size
```

## 남은 검증 및 판정

코드 회귀 검사 통과, 실제 화면·플레이 검증 미완료.

이번 실행 환경의 브라우저 도구가 http://localhost:8080 접속을 ERR_BLOCKED_BY_CLIENT로 차단했다. 따라서 수정된 UI의 실제 픽셀, Chrome/Firefox 각각의 배포본 실행, 실제 모바일 터치, 10스테이지 두 번 완주와 3분 난이도 검증은 통과로 판정하지 않는다. 접속 가능한 브라우저 환경에서 이 항목들을 이어서 검사해야 한다. 로컬 서버는 실행해 둔다.

사용한 검사 지침: js13k-playtest-reviewer.

## 추가 수정 — 유니콘·음악·증원·속도

### 사용자 요청과 적용

- 유니콘 PNG와 WebP의 보이는 픽셀을 비교해 차이가 0임을 확인했다. 압축 손상으로 보지 않는다. 기존 48×48·모든 방향·무지개 갈기·작은 검은 점 눈과 에셋 파일은 유지했다.
- 유니콘의 소각도 회전을 제거하고 몸통/다리 소스 영역의 중복 절단을 없앴다. 8방향 모두 다리 두 그룹을 교대로 움직이며, 옆면 꼬리 영역은 별도로 유지한다. 중립 보행 위상은 온전한 원본 스프라이트와 픽셀 단위로 같다. 큰 원본 재제작·다운샘플링은 적용하지 않았다.
- 음악은 기존 Web Audio 시퀀서의 저음·화음·리드·타악을 보강했다. 배경 1–2 / 3–5 / 6–8 / 9–10에 맞춰 132 / 144 / 156 / 168 BPM으로 바뀐다. 종료된 oscillator/gain을 disconnect하며 기존 입력 잠금 해제·M 음소거를 유지한다.
- 플레이 시간 5초마다 해금된 종류별 1마리씩 서로 다른 맵 변에서 증원한다. Stage 1–3은 근거리, 4–6은 근거리+원거리, 7–10은 세 종류다. 중심은 경계에서 48px 안쪽이며 플레이어와 160px 이상 떨어진다. 강화·실패·완료에서는 증원하지 않으며 재시작/스테이지 이동 때 카운터를 초기화한다.
- 적 속도 = Stage 1 기준 200/180/160 + (표시 스테이지−1)×10 px/s. 원거리 Stage 4=210, 대형 Stage 7=220, Stage 10=290/270/250. 감속 오라는 증가 후 속도의 35%. 적 탄환은 속도 300px/s·수명 2초 유지.
- 대시보드의 속도·증원 계산을 갱신하고 원본 크기 화면에 제자리 보행 및 선택 스테이지 음악 비교 버튼을 추가했다. 생성기는 `node tools/build-dashboard.mjs`이며 대시보드는 배포 ZIP에서 제외된다.

### 실행 검증

- check/debug/zip/size 각각 최종 코드로 2회 성공. zip 명령 안의 production 빌드도 각 회 성공.
- 기존 회귀: 회차당 방향 입력 1,152건, HUD 논리 좌표 14경우, 두 번 터치 확정 — 2회 통과.
- 신규 회귀: 회차당 10스테이지×32개 결정적 난수 시나리오에서 5/10/15초 증원·같은 회차의 변 중복 없음·플레이어 안전 거리·5초 경계 중복 방지·비플레이 정지 — 2회 통과.
- 속도: 10스테이지×3종류×감속 유무 60경우 — 2회 통과. 대형의 기존 가속/방향 전환 보간이 수렴한 속도를 비교했다.
- 실제 Game 클래스의 프레임 실행에서 10스테이지의 강화 중 타이머 정지·재시작 증원 카운터 초기화·초기 적 수 복원·누적 시간 보존·전체 재시작 시간 초기화 — 2회 통과.
- 그리기 호출 검사: 8방향×4위상에서 회전 없음·정수 위치·원본/출력 크기 동일 — 2회 통과.
- 별도 native Canvas(@napi-rs/canvas) 실행: 각 8방향에서 중립 보행 픽셀=온전한 원본, 서로 반대인 다리 위상의 픽셀 차이, Canvas transform 복원 — 2회 통과. 32개 방향/위상 표를 `/tmp/upe-unicorn-motion.png`로 렌더링해 시각 확인했다. 브라우저 렌더링은 아니다.
- 오디오 API 대역 실행: 입력 전 미생성·입력 반복 시 컨텍스트 1개·M 음소거·종료 노드 해제 — 2회 통과. 32스텝 음표 수는 배경별 120/148/196/204, 스텝 간격은 약 0.227/0.208/0.192/0.179초. 실제 음악 청음은 아니다.
- 대시보드 Node VM 상태 검사: 회차당 3프리셋×10스테이지, 순차 강화 편집 9번, 독립적인 동적 계획법과 수집 경로 비교, 시간·증원 가정 변경 — 2회 통과. DOM 대역 검사이며 실제 클릭 검사가 아니다.
- 기존 localhost:8080 서버의 HTTP 응답이 갱신된 debug HTML과 바이트 단위로 같음을 확인했다. 새 서버 시작은 기존 포트 사용으로 EADDRINUSE였으므로 기존 서버를 종료하거나 교체하지 않았다.

재실행:

```sh
npm run check
npm run debug
npm run zip
npm run size
./node_modules/.bin/esbuild tools/change-check.ts --bundle --platform=node --format=esm --loader:.webp=dataurl --define:DEBUG=false --outfile=/tmp/upe-change-check.mjs
node /tmp/upe-change-check.mjs
./node_modules/.bin/esbuild tools/regression-check.ts --bundle --platform=node --format=esm --loader:.webp=dataurl --define:DEBUG=false --outfile=/tmp/upe-regression.mjs
node /tmp/upe-regression.mjs
node tools/build-dashboard.mjs
```

### ZIP 측정

| 단계 | ZIP bytes | 직전 대비 |
| --- | ---: | ---: |
| 변경 전 재빌드 | 12,887 | — |
| 유니콘 렌더링 | 12,893 | +6 |
| 음악 편곡 | 12,900 | +7 |
| 증원·속도·초기화 | 13,063 | +163 |
| 오디오 종료 노드 정리 | 13,077 | +14 |

총 +190바이트, 제한 13,312바이트 대비 235바이트 여유. 최종 2회 측정값 동일.

### 미검증과 다음 확인

실제 게임 http://localhost:8080/ 접속을 다시 시도했지만 브라우저 도구가 ERR_BLOCKED_BY_CLIENT로 차단했다. 브라우저별 실제 픽셀·입력·음악 청음·모바일 터치·완주 난이도는 미검증이다. 기존 에셋의 픽셀 보존과 움직임 프레임 차이가 사용자의 시각적 만족이나 자연스러운 보행을 보장하지 않는다. 대시보드에서 전후 인상을 확인한 뒤 필요하면 추가 조정한다.

후반 근거리 목표 속도 290px/s는 속도 강화 플레이어 288px/s보다 높다. 지속 증원으로 적이 누적되는 요청을 그대로 구현했으며 개체 수의 임의 상한을 추가하지 않았다. 후반 회피 가능성·장시간 프레임 성능·약 3분 목표는 실제 완주로 판단해야 한다. 대시보드의 시간 모델은 이 적 압박을 별도로 수치화하지 않는다.

## 난이도 완화 및 4방향 탄환 — a113256 이후

이 절은 위의 5초 무제한 증원·스테이지당 +10px/s 기록을 대체하는 최신 동작이다.

- 적 속도 증가를 스테이지당 +5px/s로 완화. Stage 10 근거리/원거리/대형은 245/225/205px/s.
- 증원 주기 8초, 상한은 초기 적 수 + 해금 종류 수 × 3. Stage 1–10은 4/7/9/15/17/19/24/27/30/35마리.
- 생존 적만 상한 계산에 포함한다. 같은 프레임에 소비 표시된 적은 제외한다. 상한에서 지난 회차를 건너뛰고 다음 회차에 부족한 자리만 보충한다. 각 회차에는 종류당 최대 1마리, 부족한 자리는 마지막으로 추가한 종류 다음부터 순환한다. 순환 인덱스도 스테이지 시작/재시작에 초기화한다.
- RAINBOW BOLT는 바라보는 방향에 관계없이 유니콘에서 상하좌우 4발 동시 발사. 기존 간격 1초·속도 420px/s·수명 1초·피해량 유지. 적 탄환은 변경하지 않았다.
- tools/change-check.ts를 확장했다. 회차당 기존 증원 시나리오 320개·속도 60경우·10개 스테이지 프레임/재시작 검사에 더해, 10스테이지 상한/처치/종류 순환/밀린 증원 없음, 서로 다른 바라보기 3경우의 4방향 위치·속도·발사 간격·강화 중 정지를 검사했다. 2회 통과.
- 기존 스킬/HUD 회귀 검사와 check/debug/zip/size를 각각 2회 통과. ZIP은 13,077 → 13,148바이트(+71), 제한 대비 164바이트 여유. 최종 두 번 동일.
- 대시보드에 최신 속도·8초 주기·상한·4방향 스킬 설명 반영. Node VM 검사 2회 통과.
- 검증은 Node에서 실제 게임 소스를 실행한 상태/호출 검사이며 Canvas/Audio/DOM 대역을 사용한다. 이번 변경의 실제 브라우저 플레이·모바일·완주 난이도는 미검증이다. 앞서 기록한 브라우저 차단을 우회하지 않았다.

## 탄환·오라 조합 추가 조정

- 4방향 무지개 탄환 발사 간격을 2초로 변경했다. 속도 420px/s·수명 1초·4방향 동시 발사는 유지한다.
- 오라 안의 적 이동 속도는 해당 스테이지 원래 속도의 75%(25% 감속)로 변경했다. 반경 220px 및 적 탄환 35% 속도(105px/s)는 유지한다.
- 오라 테두리와 감속된 적/탄환의 외곽선을 #75fff044, 약 27% 불투명도로 변경했다. 기존 오라 내부 채움은 유지한다.
- 신규 회귀에서 2초 발사 경계, 오라 적 속도 60경우, 적 탄환의 오라 안/밖 105/300px/s, 오라 strokeStyle을 검사했다. 기존 증원·상한·순환·재시작·HUD·스킬 검사와 함께 2회 통과했다. Node API 대역 실행이다.
- 별도 native Canvas에서 오라를 투명 배경에 렌더링해 중심 alpha 24, 전체 최대 alpha 86로 반투명을 확인했다. 2회 통과. 브라우저 픽셀 검사는 아니다.
- check/debug/zip/size 각각 2회 통과. ZIP 13,148 → 13,147바이트(-1), 남은 예산 165바이트. 대시보드도 최신 수치로 갱신했다.
- 실제 브라우저 플레이와 조합 난이도 평가는 미검증. 커밋하지 않았다.

## 오라 투명도 추가 조정

- 오라·감속 표시 외곽선 #75fff044 → #75fff022: 불투명도 약 27% → 13%.
- 오라 내부 채움 #64ffe818 → #64ffe808: 불투명도 약 9% → 3%. 범위와 감속 성능은 그대로다.
- check/debug/zip/size 및 신규 회귀 검사 각각 2회 통과. 별도 native Canvas에서 중심 alpha 8, 최대 alpha 41 확인 — 2회 통과. 실제 브라우저 시각 검사는 미검증.
- ZIP 13,147 → 13,143바이트(-4), 여유 169바이트. 대시보드 재생성 완료. 커밋하지 않았다.

## 오라 감속·범위 재조정

- 적 이동 속도 75% → 65%(35% 감속), 효과 반경 220 → 200px. 적과 적 탄환의 범위 판정 및 감속 외곽선 판정을 함께 갱신했다. 오라 시각화의 기본 반경도 200px이며 기존 ±5px 맥동을 유지한다.
- 적 탄환 속도 비율 35%, 반투명 외곽선/내부 채움, 탄환 발사 간격 2초 유지.
- 199/200/201px 경계에서 적 속도와 적 탄환 속도, 시간 0의 표시 반경 200px를 실제 소스로 검사했다. 신규 회귀 및 check/debug/zip/size 각각 2회 통과. Node API 대역 검사이며 실제 브라우저 시각·플레이 검사는 미검증.
- ZIP은 13,143바이트로 동일하며 169바이트 여유. 대시보드 갱신, 커밋하지 않음.

## 대형적 접지·보행 및 유니콘 교체 시안

- 대형적 PNG의 실제 불투명 범위는 해당 96×96 셀 안에서 (1,18)–(94,76)이다. 기존 몸 전체 bob/rotate/scale이 발을 고정 그림자에서 들어 올렸다.
- 원본 96×96 에셋은 유지하고 대형적의 전체 수직 이동·회전·스케일 변형을 제거했다. 그림자 윗줄 y+38과 바닥에 둔 발의 마지막 불투명 줄 y+38을 맞췄다. 좌우 흔들림은 그림자도 함께 따른다.
- 다리 4개 영역은 두 그룹이 교대로 최대 8px 들리고 최대 3px 앞뒤로 움직인다. 다리를 먼저 그리고 상체를 덮어 이음매를 처리한다. 모든 이미지 소스/출력 영역 크기는 동일하며 정지 시 다리 위상은 0으로 돌아간다.
- 기존 및 신규 회귀를 합친 tools/change-check.ts 2회 통과. 대형적 32개 좌우/위상 호출 검사 포함. 별도 native Canvas에서도 32개 좌우/위상마다 바닥 발과 그림자 픽셀 접촉, transform 복원을 2회 확인했다. 30개의 서로 다른 보행 렌더 결과 확인. 실제 브라우저 검사는 아니다.
- check/debug/zip/size 각 2회 통과. ZIP 13,143 → 13,137바이트(-6), 여유 175바이트.
- 대시보드 캐릭터 보행 비교가 유니콘뿐 아니라 적에도 적용되도록 갱신했다.
- 유니콘은 ImageGen으로 기존의 흰 말·무지개 갈기/꼬리·작은 검은 점 눈·5개 기본 방향을 바탕으로 새 시안을 생성했다. 투명 배경 생성이 두 번 모두 실제 alpha 대신 체크무늬로 나와 게임에 사용하지 않았다. 마지막 시안은 하늘색 배경의 디자인 검토용 이미지다.
- 유니콘 원본 src/assets 파일과 유니콘 게임 렌더링은 이번 작업에서 바꾸지 않았다. /tmp/js13k-asset-preview-unicorn/comparison.png는 기존 48px 출력과 후보 48px 출력, 2.5배 관찰용 확대를 비교한다. 최종 투명 아틀라스 준비·게임 적용·ZIP 비용은 시안 승인 후 검증한다.
- 원본 시안: /Users/harry/.codex/generated_images/01a09582-b9b9-7db3-8e1a-a1bd69cc074f/exec-657651a3-92ff-4c2b-ae96-57de0f7d01d4.png
- 새 유니콘 시안 승인 대기. 커밋하지 않음.

## 승인된 통통한 얼굴 유니콘 적용

- 사용자가 둥근 볼/주둥이, 살짝 통통한 몸통, 콧구멍 없는 최종 시안을 승인해 적용했다. 승인 시안: /Users/harry/.codex/generated_images/01a09582-b9b9-7db3-8e1a-a1bd69cc074f/exec-53c42fcc-4e81-452f-a138-ff077ebf5351.png.
- 시안과 동일한 방향별 crop을 48×48 셀로 변환하고 하늘색 외부 배경을 투명화했다. 13색과 투명색으로 정리해 단단한 픽셀 경계를 유지했다. src/assets/unicorn-atlas.png 및 lossless WebP를 교체했다. 아틀라스는 240×48, 기본 5방향과 좌우 반전으로 8방향을 표시한다. 게임 렌더러의 48×48 표시와 기존 procedural 보행은 유지했다.
- PNG/WebP를 native Canvas로 각각 디코딩하여 픽셀 완전 일치, 5개 셀의 비어 있지 않은 실루엣, 0/255 alpha, 240×48 크기를 2회 실행 확인했다. 8방향×4위상 렌더 이미지도 확인했다. 방향별 중립 보행과 원본 픽셀 일치, 교대 다리 위상의 차이, transform 복원 검사를 2회 통과했다. 이는 native Canvas 실행이며 실제 브라우저 검사는 아니다.
- tools/change-check.ts 및 tools/regression-check.ts 각 2회 통과. 대시보드의 3프리셋×10스테이지 상태와 독립 DP 경로 대조도 Node VM/DOM 대역에서 2회 통과했다.
- npm run check/debug/zip/size 각각 2회 성공. 두 ZIP 모두 13,256바이트. 직전 13,137 대비 +119, 제한 13,312 대비 56바이트 여유. WebP 자체는 1,408 → 1,514바이트.
- stage-dashboard.html을 새 에셋과 ZIP 측정으로 재생성했다. 실제 브라우저 픽셀·Chrome/Firefox 배포본·모바일 터치·완주 난이도는 기존 접속 차단으로 여전히 미검증이다. 커밋·푸시는 수행하지 않았다.

## 정면 얼굴 재승인 시안 적용

- 사용자가 승인한 최신 시안 exec-ef6498cc-ec57-4269-bd21-c1bb469fed9d.png를 적용했다. 더 통통한 얼굴을 유지하면서 정면 관자놀이를 좁히고 둥근 턱으로 이어지도록 수정한 버전이다. 콧구멍 없는 얼굴·무지개 갈기/꼬리·작은 검은 점 눈을 유지한다.
- 기존 변환과 동일한 crop/48×48 출력/13색 팔레트를 사용하고 배경색 마스크의 허용 범위를 조정해 생성 이미지의 외부 배경 잔여 픽셀을 줄였다. 아틀라스 240×48, lossless WebP 1,486바이트. 렌더러와 게임 규칙은 이번 적용에서 변경하지 않았다.
- native Canvas에서 PNG/WebP 픽셀 일치·5개 48×48 셀·0/255 투명도, 8방향 중립 보행의 원본 일치·교대 다리 위상 차이·transform 복원 각각 2회 통과. 8방향×4위상 렌더 시각 확인. 실제 브라우저 검사는 아니다.
- tools/change-check.ts, tools/regression-check.ts, 대시보드 Node VM/DOM 대역 검사 각각 2회 통과. npm run check/debug/zip/size 각각 2회 성공.
- ZIP 두 번 모두 13,225바이트: 직전 13,256 대비 -31바이트, 제한 대비 87바이트 여유. 대시보드에 새 에셋과 측정값 반영.
- 실제 브라우저·모바일·완주 검증은 여전히 미검증이며 커밋/푸시는 수행하지 않았다.

## 승인된 부드러운 축소본 적용 및 배포 압축

- 승인한 원본의 면적 평균 축소본을 기준으로 48×48 셀을 유지했다. 큰 원본에서 픽셀 하나만 선택하던 변환 대신 면적과 alpha를 가중 평균하여 얇은 윤곽 손실을 줄였다. 새 캐릭터를 그리거나 외곽선을 덧칠하지 않았다.
- 배포용은 디더링 없는 적응형 16색 팔레트와 반투명 경계를 사용한다. 거의 투명한 잔여 픽셀을 제거하고 불투명 영역을 보정했다. PNG/WebP 모두 240×48, 반투명 픽셀 310개. WebP는 1,876바이트이며 원본 PNG와 raw RGBA 값이 완전히 일치하는지 libvips/sharp 디코딩으로 2회 확인했다.
- native Canvas의 PNG/WebP 디코딩 후 RGBA 비교는 반투명 픽셀의 디코더 반올림 차이 때문에 불일치했다. 이를 통과로 처리하지 않았으며, 위 raw 디코딩 결과와 구분한다. 게임용 WebP 자체로 8방향 보행을 실행해 중립 프레임과 원본 일치, 교대 위상 차이, transform 복원을 각 2회 통과했다.
- tools/build-game.mjs: 명시적으로 열거한 게임 내부 속성만 Terser로 축약한다(브라우저 built-in 예약은 기본대로 유지). ECS 구조와 게임 기능을 변경하지 않는다. 이미 압축된 WebP의 data URL은 Roadroller 입력에서 분리한 뒤 같은 배포 script에 다시 포함한다. 고정 Roadroller 파라미터도 측정으로 갱신했다. 외부 자원·런타임 의존성은 추가하지 않았다. HTML/CSS와 tools/build-zip.mjs는 기존 설정을 유지했다.
- 임시 비교 저장소는 tsconfig가 빠져 class field 변환 등이 실제 게임 빌드와 달랐다. 그곳에서 측정한 13,253바이트는 최종 값이 아니다. 실제 게임 저장소의 최종 ZIP은 두 번 모두 12,969바이트. 직전 실제 측정 13,225 대비 -256바이트, 제한 대비 343바이트 여유.
- npm run check/debug/prod/zip/size 각각 2회 성공. 원본 회귀 검사와 배포 속성 축약을 적용한 회귀 검사도 각각 내부 2회 통과. 대시보드 Node VM 상태 검사 2회 통과.
- tools/production-smoke.mjs: 실제 build/debug.html 및 build/index.html의 script를 Node VM과 Canvas/Image 대역에서 실행했다. 타이틀, Space 시작, 600프레임을 각각 2회 통과. 최초 5초 VM 제한에서는 배포 decoder가 시간 초과했으며 45초 제한으로 재실행해 통과했다. 브라우저 로딩 시간 측정은 아니다.
- 재실행: node tools/production-regression.mjs, node tools/production-smoke.mjs. 기존 tools/change-check.ts 및 tools/regression-check.ts 명령도 유지한다.
- 대시보드에 최신 에셋/크기를 반영했다. 실제 Chrome/Firefox 화면·모바일 터치·10스테이지 완주는 미검증이며 커밋/푸시는 하지 않았다.

## 최종 배포 점검 — df0f153 기준

판정: **배포 검사 완료, 실제 플레이 검증 미완료**. 검사 중 새로 확인한 게임 결함은 없다. 이는 실제 플레이에서 결함이 없다는 판정이 아니다.

### 이번에 실제 실행한 검사

- check/debug/prod/zip/size를 각각 2회 실행했다. 두 ZIP 모두 12,969바이트, 제한 대비 343바이트 여유.
- node tools/production-regression.mjs: 속성 축약을 적용한 게임 로직·입력·HUD·증원·오라·공격·재시작 검사 내부 2회 통과. Node와 API 대역 실행이다.
- node tools/package-check.mjs: ZIP 압축 해제, CRC, index.html 단일 엔트리, 배포 HTML과 바이트 일치, 내장 WebP 3개와 원본 파일의 바이트 일치, 외부 script/stylesheet 태그 없음, sourceMappingURL 없음 — 2회 통과. 네트워크 요청 관찰 검사는 아니다.
- 별도 libvips 이미지 디코딩: 내장 WebP 3개가 각각 240×48 / 192×96 / 80×88 크기로 디코딩됨 — 2회 확인. 적 아틀라스는 혼합 셀이 들어 있는 전체 크기이며 캐릭터 출력 크기와 다르다.
- node tools/production-smoke.mjs: 실제 debug/index HTML의 타이틀·Space 시작·600프레임 실행, 디버그 상태가 debug에만 존재하고 production에서는 노출되지 않음 — 각각 2회 통과. Canvas/Image 대역을 사용하는 Node VM이며 실제 브라우저 플레이가 아니다.
- 개발 서버 http://localhost:8080/ HTTP 200 확인. 이 서버는 모든 경로에 debug.html을 반환하므로 /index.html 주소를 입력해도 배포본 검사가 되지 않는다.

### 실행할 수 없었던 검사

브라우저 도구로 http://localhost:8080/ 페이지를 새로 열었지만 다시 net::ERR_BLOCKED_BY_CLIENT가 발생했다. 정책을 변경하거나 우회 경로를 사용하지 않았다. 다음 항목은 모두 미검증이다.

- 최신 유니콘의 실제 크기 선명도·8방향 보행과 대형적 발/그림자 접지.
- 작은 화면 HUD, 스킬 상하 이동, 실제 모바일 터치 입력/가로세로 전환.
- Chrome/Firefox 각각 배포본 시작·프리즘 수집·게이트·성장·실패·재시작, 실제 콘솔과 외부 요청.
- 10스테이지 두 번 완주, 약 3분 목표, 후반 증원/속도와 공격·오라 조합의 난이도.
- 배경 구간별 음악 변화와 효과음의 실제 청음.

### 실기기에서 이어갈 순서

1. build/game.zip을 별도 폴더에 풀고 그 안의 index.html을 Chrome과 Firefox에서 각각 연다. 개발 서버의 경로만 바꿔 배포본으로 간주하지 않는다.
2. 정지/8방향 이동을 보고 유니콘 윤곽·다리, 대형적 접지를 확인한다. 화면 너비 320/390/960px에서 HUD·미니맵·성장 화면을 확인한다.
3. 공격 우선 및 능력 우선 경로로 각각 10스테이지를 완주한다. 게임 누적 타이머로 시간을 기록한다. 강화 선택 시간은 제외한다. 실패했다면 스테이지·현재 스킬·실패 원인도 기록한다. 무적이나 적 제거를 사용한 검사는 난이도 검증으로 기록하지 않는다.
4. 음악 잠금 해제, 배경 구간별 변화, M 음소거, 실패 후 재시작을 확인한다.
5. 실제 모바일에서 이동+공격 동시 터치, 손을 뗀 뒤 정지, 카드 선택·확정, 회전 후 표시/터치 위치를 확인한다.

| 회차 | 기기 / 브라우저 | 스킬 획득 순서 | 도달 스테이지 | 누적 시간 | 실패/불편 및 재현 |
| --- | --- | --- | --- | --- | --- |
| 1 | 미실행 | 공격 우선 | — | — | — |
| 2 | 미실행 | 능력 우선 | — | — | — |

자동 검사만으로 난이도를 임의 조정하지 않았다. 현재 플레이 검증이 막힌 상태이므로 제출 완료로 처리하지 않았으며, 이번 검사 도구/문서 변경은 아직 커밋·푸시하지 않았다.
